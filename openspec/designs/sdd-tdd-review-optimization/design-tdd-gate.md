# 设计文档：TDD 测试门禁

> 版本: 1.1
> 日期: 2026-05-07
> 涉及方案: A (可执行骨架) / B (强制补测试) / D (测试运行门禁)
>
> **实现状态**: 方案 A/D 已部分实现，方案 B 已实现，类型/Lint 检查和全量测试门禁待实现

## 1. 问题定义

### 1.1 当前状态

```
PreToolUse Hook 触发
    ↓
生成 test-cases-<name>.md (markdown 占位符)
    ↓
permissionDecision: "allow" (软约束)
    ↓
Claude 可以忽略，直接实现代码
    ↓
Task 完成时无测试验证
```

### 1.2 目标状态

```
Apply Task 开始
    ↓
检查是否存在对应测试文件
    ↓ (不存在)
生成可执行测试骨架 (Jest/Vitest/pytest)
    ↓
要求实现测试用例
    ↓
运行测试
    ↓ (通过)
标记 Task 完成
    ↓ (失败)
拒绝标记，提示修复
```

---

## 2. 技术方案

### 2.1 方案 A：生成可执行测试骨架

> **状态**: ✅ 已实现
>
> **实现文件**:
> - `plugin/hooks/pre-tool-openspec-test.py` — Hook 生成可执行测试
> - `plugin/utils/test-framework.py` — 框架检测
> - `plugin/templates/test-jest.js` — Jest 模板
> - `plugin/templates/test-pytest.py` — pytest 模板

#### 2.1.1 框架检测

**检测逻辑**:
```python
def detect_test_framework(project_root: str) -> tuple[str, str]:
    """
    Returns: (framework, runner_command)
    """
    package_json = os.path.join(project_root, "package.json")
    requirements_txt = os.path.join(project_root, "requirements.txt")
    pyproject_toml = os.path.join(project_root, "pyproject.toml")

    if os.path.exists(package_json):
        # Node.js project
        with open(package_json) as f:
            pkg = json.load(f)
            deps = {**pkg.get("dependencies", {}),
                    **pkg.get("devDependencies", {})}

            if "vitest" in deps:
                return ("vitest", "npx vitest run")
            if "jest" in deps:
                return ("jest", "npm test")
            if "mocha" in deps:
                return ("mocha", "npx mocha")
            # Default to jest
            return ("jest", "npm test")

    if os.path.exists(pyproject_toml):
        # Python project with pyproject.toml
        return ("pytest", "pytest")

    if os.path.exists(requirements_txt):
        # Python project with requirements.txt
        with open(requirements_txt) as f:
            content = f.read()
            if "pytest" in content:
                return ("pytest", "pytest")

    # Fallback
    return ("unknown", "")
```

#### 2.1.2 测试骨架模板

**Jest/Vitest 模板**:
```javascript
// tests/<module>.test.js
// Auto-generated by wps-claude-plugin
// Task: <task_description>

describe('<Module Name>', () => {
  // === Task <N>: <task_description> ===

  describe('Happy Path', () => {
    it('should <expected_behavior>', async () => {
      // Arrange
      // const input = ...;

      // Act
      // const result = await functionUnderTest(input);

      // Assert
      // expect(result).toBe(expected);
      expect(true).toBe(true); // TODO: Replace
    });
  });

  describe('Error Cases', () => {
    it('should return error for invalid <input>', async () => {
      // Arrange
      // const invalidInput = ...;

      // Act & Assert
      // await expect(functionUnderTest(invalidInput))
      //   .rejects.toThrow('Expected error');
      expect(true).toBe(true); // TODO: Replace
    });
  });

  describe('Edge Cases', () => {
    it('should handle empty/null input', async () => {
      // Arrange
      // const emptyInput = null;

      // Act & Assert
      // expect(() => functionUnderTest(emptyInput))
      //   .toThrow();
      expect(true).toBe(true); // TODO: Replace
    });
  });
});
```

**pytest 模板**:
```python
# tests/test_<module>.py
# Auto-generated by wps-claude-plugin
# Task: <task_description>

import pytest

class Test<ModuleName>:
    """Tests for <module_name> module."""

    # === Task <N>: <task_description> ===

    class TestHappyPath:
        def test_<expected_behavior>(self):
            # Arrange
            # input_data = ...

            # Act
            # result = function_under_test(input_data)

            # Assert
            # assert result == expected
            assert True  # TODO: Replace

    class TestErrorCases:
        def test_invalid_<input>(self):
            # Arrange
            # invalid_input = ...

            # Act & Assert
            # with pytest.raises(ValueError):
            #     function_under_test(invalid_input)
            assert True  # TODO: Replace

    class TestEdgeCases:
        def test_empty_null_input(self):
            # Arrange & Act & Assert
            # with pytest.raises(TypeError):
            #     function_under_test(None)
            assert True  # TODO: Replace
```

#### 2.1.3 实现方式

**Option 1: Hook 生成 (当前方案增强)**

修改 `pre-tool-openspec-test.py`:

```python
def generate_executable_test(change_name: str, task: str,
                             framework: str, test_dir: str) -> str:
    """Generate executable test file based on detected framework."""

    module_name = extract_module_name(task)
    test_file = os.path.join(test_dir, f"{module_name}.test.js")

    # Generate based on framework
    if framework == "jest" or framework == "vitest":
        content = generate_jest_test(module_name, task)
    elif framework == "pytest":
        content = generate_pytest_test(module_name, task)
    else:
        # Fallback to markdown
        content = generate_markdown_template(change_name, [task])

    return test_file, content
```

**Option 2: Skill 内嵌入 (推荐)**

在 `openspec-apply-change` SKILL.md 中增加：

```markdown
### Step 6.5: Generate Test Skeleton (Before Implementation)

For each pending task:
1. Extract module/file name from task description
2. Check if test file exists in tests/ or __tests__/
3. If not exists:
   - Detect test framework (package.json / pyproject.toml)
   - Generate executable test skeleton
   - Write to tests/<module>.test.js
   - Prompt: "Test skeleton generated. Please implement tests FIRST."
```

**推荐方案 2**：因为 Skill 层面可以控制流程，Hook 层面日志有限。

---

### 2.2 方案 D：测试运行门禁

> **状态**: ⚠️ 部分实现
>
> **已实现**:
> - SKILL.md 内嵌测试运行步骤
> - 测试失败时阻断 Task 完成
> - 支持 Jest/Vitest/pytest 测试运行
>
> **未实现**:
> - 类型/Lint 检查集成
> - 全量测试门禁（所有 Task 完成后）
>
> **实现文件**:
> - `plugin/skills/openspec-apply-change/SKILL.md` — TDD Gate 步骤
> - `plugin/utils/test-runner.py` — 测试运行逻辑

#### 2.2.1 流程设计（测试范围驱动）

```
┌─────────────────────────────────────────────────────────┐
│              Apply Task Workflow (测试范围驱动)           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Read task T from tasks.md                           │
│      ↓                                                  │
│  2. 【测试范围识别】Identify affected modules/files      │
│      - 解析 task 描述，提取模块/功能关键词               │
│      - 定位相关源文件（如 routes/auth.js）              │
│      - 映射到测试范围（如 tests/auth.test.js）          │
│      ↓                                                  │
│  3. 【测试补充/更新】Check & supplement test cases       │
│      ├─ 无测试 → 生成测试骨架 (方案 A)                   │
│      ├─ 有测试但覆盖不全 → 补充测试用例                  │
│      └─ 测试完整 → 继续                                  │
│      ↓                                                  │
│  4. 【实现代码】Implement task (code changes)            │
│      ↓                                                  │
│  5. 【运行范围测试】Run ONLY scoped tests                │
│      - 仅运行步骤 2 识别的测试范围                       │
│      - 不运行全量测试（性能优化）                        │
│      ├─ Pass → Mark task complete ✓                    │
│      └─ Fail → BLOCK: Show error, require fix           │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

#### 2.2.2 测试范围识别

**核心原则**：每个 task 开始前，先识别测试范围，只运行相关测试，不运行全量测试。

**识别策略**：

```python
def identify_test_scope(task: str, src_dir: str, tests_dir: str) -> dict:
    """
    Identify test scope for a task.
    
    Returns:
        {
            "affected_files": list[str],     # 受影响的源文件
            "test_files": list[str],         # 对应的测试文件
            "test_patterns": list[str],      # 测试名称匹配模式
            "needs_new_tests": bool          # 是否需要生成新测试
        }
    """
    affected_files = []
    test_files = []
    needs_new_tests = False
    
    # Strategy 1: Explicit file mentioned
    # Task: "Implement user registration in auth.js"
    file_match = re.search(r'(?:in|modify|edit|update)\s+([a-zA-Z_]+\.[a-z]+)', task)
    if file_match:
        filename = file_match.group(1)
        src_file = os.path.join(src_dir, filename)
        test_file = map_to_test_file(src_file, tests_dir)
        affected_files = [src_file]
        test_files = [test_file] if os.path.exists(test_file) else []
        needs_new_tests = not os.path.exists(test_file)
    
    # Strategy 2: Feature module inference
    # Task: "Add JWT authentication"
    if not affected_files:
        keywords = extract_keywords(task)
        for kw in keywords:
            possible_file = os.path.join(src_dir, f"{kw}.js")
            if os.path.exists(possible_file):
                test_file = map_to_test_file(possible_file, tests_dir)
                affected_files.append(possible_file)
                if os.path.exists(test_file):
                    test_files.append(test_file)
                else:
                    needs_new_tests = True
    
    # Strategy 3: Pattern inference
    # Task: "Implement routes for user management"
    if not affected_files:
        if "routes" in task.lower():
            for f in glob.glob(os.path.join(src_dir, "routes", "*.js")):
                affected_files.append(f)
                test_file = map_to_test_file(f, tests_dir)
                if os.path.exists(test_file):
                    test_files.append(test_file)
    
    # Extract test patterns from task
    test_patterns = extract_test_patterns(task)
    
    return {
        "affected_files": affected_files,
        "test_files": test_files,
        "test_patterns": test_patterns,
        "needs_new_tests": needs_new_tests
    }


def extract_test_patterns(task: str) -> list[str]:
    """Extract test name patterns from task description."""
    patterns = []
    
    # Extract function/feature names
    # Task: "Implement login function" → pattern: "login"
    name_match = re.findall(r'(?:implement|add|create)\s+(\w+)', task, re.I)
    patterns.extend(name_match)
    
    # Extract behavior descriptions
    # Task: "should validate email format" → pattern: "validate email"
    behavior_match = re.findall(r'should\s+(.+?)(?:\s+in|\s*$)', task, re.I)
    patterns.extend(behavior_match)
    
    return patterns
```

#### 2.2.3 测试运行（范围限定）

**核心原则**：只运行测试范围内的用例，不运行全量测试。

**Jest/Vitest（范围运行）**:
```bash
# Run specific test file (from identified scope)
npx jest tests/auth.test.js --passWithNoTests

# Run tests matching pattern (from extracted patterns)
npx jest --testNamePattern="login|validate" --passWithNoTests

# Run multiple scoped test files
npx jest tests/auth.test.js tests/user.test.js --passWithNoTests
```

**pytest（范围运行）**:
```bash
# Run specific test file (from identified scope)
pytest tests/test_auth.py -v

# Run tests matching pattern (from extracted patterns)
pytest tests/ -k "login or validate" -v

# Run multiple scoped test files
pytest tests/test_auth.py tests/test_user.py -v
```

**运行策略对比**：

| 场景 | 运行范围 | 命令示例 |
|------|----------|----------|
| 单个 task 开发 | 仅识别的测试范围 | `jest tests/auth.test.js` |
| 所有 task 完成 | 全量测试套件 | `jest` / `pytest` |
| CI/PR 检查 | 全量测试套件 | `jest --coverage` / `pytest --cov` |

#### 2.2.4 SKILL.md 改造（测试范围驱动）

**当前 apply-change 流程**:
```markdown
For each pending task:
- Show which task is being worked on
- Make the code changes required
- Mark task complete in the tasks file: - [ ] → - [x]
```

**改造后流程（测试范围驱动）**:
```markdown
For each pending task:
- Show which task is being worked on
- **Step 1: 识别测试范围**:
  - 解析 task 描述，提取模块/功能关键词
  - 定位受影响的源文件
  - 映射到对应的测试文件
  - 提取测试名称匹配模式
- **Step 2: 补充/更新测试用例**:
  - 检查测试文件是否存在
  - 不存在 → 生成测试骨架，提示先写测试
  - 存在但不完整 → 补充缺失的测试用例
  - 存在且完整 → 继续
- **Step 3: 实现代码**:
  - Make the code changes required
- **Step 4: 运行范围测试**:
  - 仅运行 Step 1 识别的测试范围
  - 使用 testNamePattern 或指定文件
  - If tests fail → BLOCK: show errors, do not mark complete
  - If tests pass → continue
- Mark task complete in the tasks file: - [ ] → - [x]
- Continue to next task
```

#### 2.2.5 错误处理

```markdown
### Test Failure Handling

When tests fail:
1. Display test output (show FAILING_TESTS env var)
2. Pause implementation
3. Offer options:
   - [1] Fix the test/solution
   - [2] Mark as blocked, explain issue
   - [3] Skip test validation (requires user confirmation)
4. Do NOT mark task complete until tests pass
```

---

### 2.3 方案 B：实现后强制补测试

#### 2.3.1 触发点

| 触发时机 | 方式 | 可靠性 |
|----------|------|--------|
| PreToolUse (Bash git commit) | Hook 检测 | 中等 |
| Apply-change task 完成点 | Skill 内嵌 | 高 |
| PostToolUse (Write) | 不支持 | N/A |

**推荐**: 在 apply-change skill 内嵌，而非依赖 Hook。

#### 2.3.2 检测逻辑

```python
def check_test_coverage(task: str, changes: list[str], tests_dir: str) -> dict:
    """
    Check if changes have corresponding tests.

    Returns:
        {
            "has_tests": bool,
            "missing_tests": list[str],
            "existing_tests": list[str]
        }
    """
    missing_tests = []
    existing_tests = []

    for changed_file in changes:
        # Map source file to test file
        test_file = map_to_test_file(changed_file, tests_dir)

        if os.path.exists(test_file):
            existing_tests.append(test_file)
        else:
            missing_tests.append(test_file)

    return {
        "has_tests": len(missing_tests) == 0,
        "missing_tests": missing_tests,
        "existing_tests": existing_tests
    }

def map_to_test_file(source_file: str, tests_dir: str) -> str:
    """Map source file to expected test file path."""
    # src/routes/auth.js → tests/routes/auth.test.js
    # src/auth.py → tests/test_auth.py

    basename = os.path.basename(source_file)
    name, ext = os.path.splitext(basename)

    if ext in (".js", ".ts", ".jsx", ".tsx"):
        return os.path.join(tests_dir, f"{name}.test{ext}")
    elif ext == ".py":
        return os.path.join(tests_dir, f"test_{name}.py")

    return os.path.join(tests_dir, f"test_{name}.{ext}")
```

#### 2.3.3 SKILL.md 增强逻辑

```markdown
### Step 6.6: Verify Test Coverage (After Implementation)

After code changes for task T:

1. Get list of changed files (git diff --name-only)
2. For each changed file in src/:
   - Check if test file exists
   - If missing → prompt: "No test for {file}. Write test first?"
3. If tests exist:
   - Run tests
   - Verify tests execute (not all skipped)
4. Report coverage status before marking complete
```

---

## 3. 文件改造清单

### 3.1 需要修改的文件

| 文件 | 改动 | 影响 |
|------|------|------|
| `plugin/skills/openspec-apply-change/SKILL.md` | 增加 Step 6.5/6.6 | 核心流程 |
| `plugin/hooks/pre-tool-openspec-test.py` | 生成可执行测试骨架 | Hook 增强 |

### 3.2 需要新增的文件

| 文件 | 用途 |
|------|------|
| `plugin/utils/test-framework.py` | 检测测试框架工具模块 |
| `plugin/templates/test-jest.js` | Jest 测试骨架模板 |
| `plugin/templates/test-pytest.py` | pytest 测试骨架模板 |

---

## 4. 兼容性考虑

### 4.1 项目无测试框架

**处理**:
1. 检测不到测试框架时，fallback 到 markdown 模板
2. 提示用户安装测试框架
3. 提供 `npm install --save-dev jest` 或 `pip install pytest` 命令

### 4.2 测试框架自定义配置

**处理**:
1. 读取 jest.config.js / pytest.ini / vitest.config.ts
2. 提取 testMatch / testPathIgnorePatterns
3. 按配置生成测试文件路径

### 4.3 跳过测试的合理场景

允许用户主动跳过测试验证：
- 纯配置文件修改 (.gitignore, README.md)
- 样式文件修改 (.css, .scss)
- 文档文件修改 (.md)

---

## 5. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 测试框架检测错误 | 生成错误模板 | 多维度检测，fallback 机制 |
| 影响文件推断不准 | 测试覆盖率检查遗漏 | 启发式 + 用户确认 |
| 测试运行超时 | 流程卡死 | 设置 60s 超时，跳过并警告 |
| 强制测试降低效率 | 开发体验变差 | 允许用户确认跳过 |

---

## 6. 验收标准

- [x] 检测项目测试框架 (Jest/Vitest/pytest)
- [x] 生成对应框架的可执行测试骨架
- [x] apply-change 流程中运行测试
- [x] 测试失败时阻止 task 标记完成
- [ ] 识别测试范围（受影响模块/文件映射到测试文件）
- [ ] 补充/更新测试用例（基于测试范围）
- [ ] 范围测试运行（仅运行识别的测试范围，非全量）
- [ ] 记录测试覆盖率报告到 test-reports/
- [ ] 类型/Lint 检查集成 (eslint/mypy)
- [ ] 全量测试门禁（所有 Task 完成后运行全量测试）

---

## 7. 待实现：类型/Lint 检查集成

### 7.1 目标

在 TDD 循环中加入类型检查和代码规范检查，形成更完整的质量门禁。

### 7.2 设计

```
Apply Task Workflow (增强版)
    ↓
实现代码
    ↓
┌───────────────────────┐
│ 类型/Lint 检查         │
│ ├─ Node.js: eslint    │
│ ├─ Python: mypy/ruff  │
│ └─ 通用: prettier     │
└───────────┬───────────┘
            ↓
    ┌─── 通过 ───┐
    │            │
    ↓            ↓
  运行测试    不通过 → 阻断，显示错误
```

### 7.3 检测逻辑

```python
def detect_linter(project_root: str) -> tuple[str, str]:
    """Detect linter/type checker in use."""
    # Node.js
    package_json = os.path.join(project_root, "package.json")
    if os.path.isfile(package_json):
        pkg = json.load(open(package_json))
        deps = {**pkg.get("devDependencies", {})}
        if "eslint" in deps:
            return ("eslint", "npx eslint")
        if "biome" in deps:
            return ("biome", "npx biome check")

    # Python
    pyproject = os.path.join(project_root, "pyproject.toml")
    if os.path.isfile(pyproject):
        return ("ruff", "ruff check")

    return ("unknown", "")
```

### 7.4 SKILL.md 改造

在 `openspec-apply-change/SKILL.md` 的 Test Gate 步骤前插入：

```markdown
- **Run lint/type check** (Lint Gate):
  - Detect linter: eslint (Node.js), ruff/mypy (Python)
  - Execute: `npx eslint <file>` or `ruff check <file>`
  - If errors found → BLOCK: show lint errors, do NOT mark task complete
  - If only warnings → proceed with warning
```

### 7.5 文件改造清单

| 文件 | 改动 |
|------|------|
| `plugin/utils/test-framework.py` | 增加 `detect_linter()` 函数 |
| `plugin/skills/openspec-apply-change/SKILL.md` | Step 6 增加 Lint Gate |

---

## 9. 待实现：前移检测 + 即时修复 (方案 O)

### 9.1 目标

把 Lint/Type 检测从 commit 时前移到每个 task 完成时，发现问题即时自动修复，减少 commit 时的阻断率。

### 9.2 当前问题

```
当前流程:
  [写代码] → [写更多代码] → ... → [commit] → ❌ lint 错误 deny → 人为干预

问题: Lint/Type 错误在 commit 时才检测，阻断率高
```

### 9.3 优化后流程（测试范围驱动 + 即时检测）

```
优化后流程:
  [识别测试范围] → [补充/更新测试用例] → [实现代码] → [即时 lint/type 检查]
                                                        ├─ 可自动修复 → [eslint --fix / ruff --fix] → 继续
                                                        └─ 不可自动修复 → [提示修复] → 修复后继续
                                                        ↓
                                                    [运行范围测试] → [标记 task 完成]
                                                        ↓
                                                    ... → [所有 task 完成] → [全量测试] → [commit] → ✅ pass
```

### 9.4 SKILL.md 改造

在 `openspec-apply-change/SKILL.md` Step 6 的实现循环中增加即时检测：

```markdown
For each pending task:
- Show which task is being worked on
- **识别测试范围**:
  - 解析 task 描述，定位受影响模块/文件
  - 映射到对应测试文件
  - 提取测试名称匹配模式
- **补充/更新测试用例**:
  - 检查测试文件是否存在，不存在则生成骨架
  - 检查已有测试覆盖，不完整则补充
- Make the code changes required
- **即时 Lint/Type 检查** (Instant Check Gate):
  - After code changes, immediately run: `npx eslint --fix <file>` or `ruff check --fix <file>`
  - If auto-fixable errors → fix automatically, continue
  - If unfixable errors → BLOCK: show errors, do NOT mark task complete
  - If only warnings → proceed
- **运行范围测试** (Scoped Test Gate):
  - 仅运行识别的测试范围（非全量）
  - Execute: `npx jest <scoped-files> --testNamePattern=<pattern>` or `pytest <scoped-files> -k <pattern>`
  - If tests fail → BLOCK: show errors, do not mark complete
  - If tests pass → continue
- Mark task complete
```

### 9.5 文件改造清单

| 文件 | 改动 |
|------|------|
| `plugin/skills/openspec-apply-change/SKILL.md` | Step 6 增加即时检测步骤 |
| `plugin/hooks/pre-tool-commit-review.py` | Phase 1 检测逻辑不变，但预期阻断率下降 |

### 9.6 预期效果

- Lint/Type 错误在 task 完成时即时修复，不再到 commit 时才阻断
- commit 时的 lint/type 阻断率预计从 ~15% 降至 <2%

---

## 10. ERROR → Task 闭环设计 (方案 P)

### 10.1 目标

Review 发现的所有 ERROR（不区分类型）生成修复 task，重入 apply 循环，形成自动闭环。

### 10.2 流程设计

```
Apply 所有 Task 完成
    ↓
Code Review
    ├─ 无 ERROR → Archive ✓
    └─ 有 ERROR → 生成修复 task
                    ↓
              追加到 tasks.md
                    ↓
              重新进入 Apply
                    ↓
              修复 Task 完成
                    ↓
              重新 Review
                    ↓
              无 ERROR → Archive ✓
```

### 10.3 Task 生成规则

每个 ERROR 生成一个修复 task：

```markdown
- [ ] Fix: <简短描述> (<file>:<line>)
  - **Category**: <Logical/Security/NullHandling/...>
  - **Current**: `<问题代码>`
  - **Suggested**: `<修复建议>`
  - **Review**: test-reports/code-review-<timestamp>.md
```

### 10.4 SKILL.md 改造

在 `openspec-apply-change/SKILL.md` 增加 Post-Review Loop 步骤：

```markdown
### Step 8: Post-Review Loop

After Code Review:

1. **Parse Review Result**:
   - Extract all ERROR entries from review report

2. **If No ERROR**:
   - Proceed to Archive

3. **If Has ERROR**:
   - Check loop count (max 3)
   - If >= 3: PAUSE and prompt manual intervention
   - Else:
     - Generate fix tasks from ERROR entries
     - Append to tasks.md
     - Increment loop count
     - Continue to Step 6 (Implement tasks)
```

### 10.5 预期效果

- 阻断率从 ~30% 降至 <1%
- 平均 1-2 轮 Review 即可通过
- 仅极端情况需要人工干预

---

## 8. 待实现：全量测试门禁

### 8.1 目标

所有 Task 完成后，运行全量测试（不只是当前 Task 的测试范围），确保没有回归。

**重要区分**：
- **范围测试**：单个 task 完成时运行，仅覆盖该 task 影响的测试范围
- **全量测试**：所有 task 完成后运行，覆盖整个项目测试套件

### 8.2 设计

```
所有 Task 完成
    ↓
运行全量测试 (npm test / pytest)
    ↓
┌─── 通过 ───┐
│            │
↓            ↓
Code Review  不通过 → 阻断，显示失败测试
```

### 8.3 SKILL.md 改造

在 `openspec-apply-change/SKILL.md` Step 7 (Auto Review) 前插入：

```markdown
- **Run full test suite** (Full Test Gate):
  - Execute: `npm test` or `pytest`
  - If any tests fail → BLOCK: show all failing tests, do NOT proceed to review
  - If all pass → proceed to Step 7 (Auto Review)
  - Save test results summary to test-reports/
```

### 8.4 测试运行策略总结

| 阶段 | 运行范围 | 命令 | 目的 |
|------|----------|------|------|
| 单个 task | 范围测试 | `jest tests/auth.test.js` | 快速验证，即时反馈 |
| 所有 task 完成 | 全量测试 | `jest` / `pytest` | 回归检测，整体质量 |
| CI/PR | 全量测试 + 覆盖率 | `jest --coverage` | 质量门禁，覆盖率报告 |

### 8.5 文件改造清单

| 文件 | 改动 |
|------|------|
| `plugin/skills/openspec-apply-change/SKILL.md` | Step 7 前增加 Full Test Gate |
| `plugin/utils/test-runner.py` | 增加 `run_full_suite()` 方法 |