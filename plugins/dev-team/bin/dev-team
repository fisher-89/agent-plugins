#!/usr/bin/env bash
# Wrapper for embedded dev-team CLI (standalone bundle)
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
BUNDLE="$SCRIPT_DIR/dev-team-bundle.js"

if [ ! -f "$BUNDLE" ]; then
  echo "错误: dev-team-bundle.js 未找到，请先执行 npm run build" >&2
  exit 1
fi

exec node "$BUNDLE" "$@"
