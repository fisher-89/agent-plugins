{
  "mcpServers": {
    "dev-team": {
      "command": "node ${CLAUDE_PLUGIN_ROOT}/bin/dev-team-mcp.cjs",
      "cwd": "${workspaceFolder}",
      "env": {
        "CLAUDE_PROJECT_DIR": "${workspaceFolder}"
      }
    }
  }
}