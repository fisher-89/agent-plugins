# 测试设计: {{change_name}}

> **日期**: {{date}}

---

## 验收范围

<!-- 用模板表格逐条映射 proposal.md 的每个 AC：
  - `AC ID`：proposal 中的编号
  - `验收条件`：原文摘录
  - `测试类型`：`单元测试` 或 `集成测试`
  - `测试文件`：目标测试文件路径
  - `测试对象/测试场景`：对应的 describe/it 标题 -->

| AC ID | 验收条件 | 测试类型 | 测试文件 | 测试对象/测试场景 |
|--------|---------|---------|----------|----------|
| {{proposal_ac_id}} | {{proposal_criterion}} | 单元测试/集成测试 | {{test_file}} | {{describe_title}} |

---

## 单元测试

<!-- 覆盖所有可在进程内验证的场景（Rust #[test]、前端 Vitest 纯函数/组件 mock 测试）。 -->

### 用例

<!-- - `测试文件`：文件路径
   - `测试对象`：describe 标题（被测函数/组件/模块）
   - `路径类型`：`正向`（happy path）、`异常`（error/invalid input）、`边界`（edge case）
   - `测试条件`：it 标题（具体输入与预期输出）
   - `迭代类型`：`新增` 或 `废弃`

   正向场景对应 proposal 的每个 AC，反向场景覆盖错误处理与无效输入，边界场景按下方参数类型映射表系统化生成。 -->

| 测试文件 | 测试对象 | 路径类型 | 测试条件 | 迭代类型 |
|---------|---------|----------|----------|----------|
| {{test_file}} | {{describe_title}} | 正向/异常/边界 | {{it_title}} | 新增/废弃 |

### Mock策略

<!-- - `测试文件`：文件路径
   - `Mock主体`：系统源代码之外的依赖（数据文件、配置文件、DB、接口、全局变量、运行环境）
   - `Mock方案`：具体 mock 的路径、输入输出、转换逻辑
   - `应用场景`：哪些 describe 需要此 mock -->

| 测试文件 | Mock主体 | Mock方案 | 应用场景 |
|---------|---------|----------|----------|
| {{test_file}} | 文件/接口/全局变量/运行环境 | {{how_to_mock}} | {{describe_titles}} |

---

## 集成测试

<!-- 覆盖跨进程/跨模块边界的场景（E2E、Rust aggregate 管线、HTTP fetch 链路）。 -->

### 用例

<!-- - `AC ID`：关联的验收标准
   - `测试文件`：文件路径
   - `测试场景`：describe 标题
   - `测试条件`：it 标题
   - `迭代类型`：`新增` 或 `废弃` -->

| AC ID | 测试文件 | 测试场景 | 测试条件 | 迭代类型 |
|--------|---------|---------|----------|----------|
| {{proposal_ac_id}} | {{test_file}} | {{describe_title}} | {{it_title}} | 新增/废弃 |


### Mock策略

<!-- 同单元测试格式，聚焦跨进程依赖（fetch、文件系统、外部命令）。 -->

| 测试文件 | Mock主体 | Mock方案 | 应用场景 |
|---------|---------|----------|----------|
| {{test_file}} | 文件/接口/全局变量/运行环境 | {{how_to_mock}} | {{describe_titles}} |

---

## 不可测试项

<!-- 列出 proposal 范围内但无法通过自动化测试验证的条目，每项说明原因。 -->

- {{item}} — **原因**: {{reason}}
