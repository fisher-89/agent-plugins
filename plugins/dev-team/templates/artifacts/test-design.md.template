# 测试设计: {{change_name}}

> **日期**: {{date}}

---

## 验收范围

<!-- 用模板表格逐条映射 proposal.md 的每个 AC：
  - `AC ID`：proposal 中`验收标准`编号
  - `验收条件`：原文摘录
  - `测试类型`：`单元测试` 或 `集成测试`
  - `测试文件`：目标测试文件路径
  - `测试对象/测试场景`：对应的 describe/it 标题，单元测试中是`测试对象`，集成测试中是`测试场景` -->

| AC ID | 验收条件 | 测试类型 | 测试文件 | 测试对象/测试场景 |
|--------|---------|---------|----------|----------|
| {{proposal_ac_id}} | {{proposal_criterion}} | 单元测试/集成测试 | {{test_file}} | {{describe_title}} |

---

## 单元测试

<!-- 覆盖所有可在进程内验证的场景（Rust #[test]、前端 Vitest 纯函数/组件 mock 测试）。
  每个源文件对应一个独立的 `### <源文件> -> <测试文件>` 章节，该文件的所有测试用例和 Mock 策略均在对应章节内集中描述。 -->

### {{源文件}} -> {{测试文件}}

<!-- 源文件和测试文件路径均相对于项目根目录。 -->
<!-- `{{源文件}}` — 被测源文件路径；`{{测试文件}}` — 对应的测试文件路径。 -->

#### 待测功能

<!-- 使用无序列表格式列出被测模块导出的函数/类/变量签名。每项格式：`- functionName(): 简短描述`。
  内容来源：grep 源文件中的导出声明（如 `export function`、`export class`、`export const`）。 -->

- {{functionName}}(): {{简短描述}}

#### 用例

<!-- 表格列定义：
  - `测试对象`：describe 标题，与验收范围中 `测试对象/测试场景` 列一致
  - `路径类型`：`正向`（happy path）、`异常`（error/invalid input）、`边界`（edge case）
  - `测试条件`：it 标题（具体输入与预期输出）
  - `迭代类型`：`新增` 或 `废弃`

  注意：不包含 `测试文件` 列（已在 heading 中声明）。
  正向场景对应 proposal 的每个 AC，反向场景覆盖错误处理与无效输入，边界场景按下方参数类型映射表系统化生成。 -->

| 测试对象 | 路径类型 | 测试条件 | 迭代类型 |
|---------|----------|----------|----------|
| {{describe_title}} | 正向/异常/边界 | {{it_title}} | 新增/废弃 |

#### Mock策略

<!-- 表格列定义：
  - `Mock主体`：系统源代码之外的依赖（数据文件、配置文件、DB、接口、全局变量、运行环境）
  - `Mock方案`：具体 mock 的路径、输入输出、转换逻辑
  - `应用场景`：哪些 describe 需要此 mock

  注意：不包含 `测试文件` 列（已在 heading 中声明）。 -->

| Mock主体 | Mock方案 | 应用场景 |
|---------|----------|----------|
| 文件/接口/全局变量/运行环境 | {{how_to_mock}} | {{describe_titles}} |

---

## 集成测试

<!-- 集成测试验证跨模块交互。单元测试通过 Mock 已覆盖模块依赖，此处聚焦模块组合时才暴露的行为。
  每项跨模块交互对应一个独立的 `### <关系标题> → <测试文件>` 章节，关系标题自由命名。 -->

### {{关系标题}} → `{{测试文件}}`

<!-- 关系标题自由命名，建议使用箭头链路描述交互方向，如 `CLI参数 → eval.json持久化`。
  后缀的 `测试文件` 路径是此关系对应的集成测试文件。 -->

**涉及模块**:

<!-- 表格强制声明此交互涉及的模块及其角色，至少包含 2 行以体现跨模块性。
  列定义：
  - `模块`：模块路径（相对于项目根目录）
  - `角色`：模块在交互中的角色，如 写入方/读取方/中间件/触发方 -->

| 模块 | 角色 |
|------|------|
| `{{module_path}}` | 写入方/读取方/中间件/触发方 |

**关联AC**: {{AC-1, AC-2}}

<!-- 列出此关系覆盖的验收标准 AC-ID（来自 proposal.md 的 `## 验收标准`）。至少一个 AC-ID。 -->

**关系描述**:

<!-- 自由文本段落。描述这些模块之间如何交互、为什么值得测试、可能的出错方式。
  不得使用列表或表格格式。 -->

{{自由文本。描述这些模块之间如何交互、为什么值得测试、可能的出错方式。}}

#### 场景: {{场景名称}}

<!-- 场景名称在同一关系下应唯一，跨关系不要求唯一。
  场景描述段落应说明：验证什么、前置条件、输入、预期输出。 -->

{{自由文本。描述该场景验证什么、前置条件是什么、输入和预期输出。}}

##### 用例

<!-- 表格列定义：
  - `路径类型`：`正向`（happy path）、`异常`（error/invalid input）、`边界`（edge case）
  - `测试条件`：it 标题（具体输入与预期输出）
  - `迭代类型`：`新增` 或 `废弃` -->

| 路径类型 | 测试条件 | 迭代类型 |
|----------|----------|----------|
| 正向 | {{happy_path_condition}} | 新增/废弃 |
| 异常 | {{error_condition}} | 新增/废弃 |
| 边界 | {{edge_condition}} | 新增/废弃 |

##### Mock策略

<!-- 仅列出跨进程边界的 Mock。内部模块间调用不 mock。
  如无 Mock 需求，省略此小节并用注释说明。
  表格列定义：
  - `Mock主体`：外部依赖名称
  - `Mock方案`：具体模拟方式
  - `应用场景`：需要此 Mock 的路径类型或测试条件 -->

| Mock主体 | Mock方案 | 应用场景 |
|----------|----------|----------|
| {{external_dep}} | {{how_to_mock}} | {{test_conditions}} |

---

## 不可测试项

<!-- 列出 proposal 范围内但无法通过自动化测试验证的条目，每项说明原因。 -->

- {{item}} — **原因**: {{reason}}
